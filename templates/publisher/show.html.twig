{% extends 'base.html.twig' %}

{% block title %}Publisher{% endblock %}

{% block body %}
    <h1>Publisher</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publisher.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ publisher.name }}</td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('publisher_index') }}">Retour à la liste</a>

    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('publisher_edit', {'id': publisher.id}) }}">edit</a>
    {% endif %}
    {{ include('publisher/_delete_form.html.twig') }}
{% endblock %}
